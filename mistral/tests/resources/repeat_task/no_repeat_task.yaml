Namespaces:
  MyService:
    actions:
      repeatable-action:
        class: std.echo
        output:
          greeting: Cheers!

Workflow:
  tasks:
    repeater_task:
      repeat:
        iterations: 0
        delay: 0
      action: MyService.repeatable-action
      publish:
        greet_msg: greeting